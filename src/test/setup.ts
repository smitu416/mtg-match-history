// ==========================================
// テスト環境のセットアップファイル
// ==========================================
// このファイルはすべてのテストが実行される前に読み込まれる。
// jest-dom の便利なマッチャー（toBeInTheDocument など）を使えるようにする。
//
// マッチャーとは「〇〇であるか確認する」ための関数のこと。
// 例: expect(element).toBeInTheDocument() = "この要素がDOMに存在する"

import '@testing-library/jest-dom';
